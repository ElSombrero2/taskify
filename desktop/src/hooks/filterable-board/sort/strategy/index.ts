import { date } from "./date";
import { id } from "./id/id";
import { priority } from "./priority-level";
import { title } from "./title";
import { username } from "./username";

export const SortStrategy = {
	id, priority, title, username, date,
};
